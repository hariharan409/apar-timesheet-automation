services:
  timesheet-automation:
    build: .
    container_name: apar-timesheet
    restart: unless-stopped
    env_file: .env
    environment:
      - TZ=Asia/Singapore
    volumes:
      - ./data:/app/data
      - ./templates:/app/templates
      - ./output:/app/output
