services:
  timesheet:
    build: .
    container_name: apar-timesheet
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./templates:/app/templates
      - ./output:/app/output
